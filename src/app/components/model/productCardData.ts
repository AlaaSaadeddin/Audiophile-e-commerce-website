import { StringMap } from '@angular/compiler/src/compiler_facade_interface';

export interface product {
    primaryTitle: string,
    mainTitle: string,
    description: String,
    buttonText: string,
}
